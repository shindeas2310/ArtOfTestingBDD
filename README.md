# ArtOfTestingBDD

This repository contains the ArtOfTestingBDD project (Cucumber / Selenium tests).

Contents:
- src/ - source and test code
- pom.xml - Maven configuration

Purpose: Push the existing local project to a new GitHub repository named `ArtOfTestingBDD`.

See CONTRIBUTING or the project README for how to run tests locally.
